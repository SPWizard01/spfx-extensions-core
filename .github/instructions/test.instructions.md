---
applyTo: "**"
---

To run test use `bun run test` command. Check `package.json` to figure out what this command does exactly.
To check coverage run `bun run coverage` command. Coverage files will be generated in the `__coverage__` directory.
